# Support

Use GitHub Issues for bug reports, packaging problems, documentation gaps, and feature requests.

For security-sensitive decoder crashes, malformed-input exploits, or resource-exhaustion reports, follow `SECURITY.md` instead of opening a public issue.

When reporting a codec bug, include the implementation (Go/Rust/Python/TypeScript), version, minimal input, and whether the behavior differs from another implementation.
