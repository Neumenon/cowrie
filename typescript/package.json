{
  "name": "cowrie-codec",
  "version": "0.9.0",
  "description": "Cowrie v1 \u2014 deterministic, content-addressable, AI-native binary codec for TypeScript (zero-copy tensors, multihash content addresses, Merkle file identity)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test src/**/*.test.ts",
    "test:coverage": "npx c8 --check-coverage --lines 80 --reporter text --reporter text-summary node --import tsx --test src/**/*.test.ts",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "json",
    "binary",
    "serialization",
    "codec",
    "tensor",
    "ml"
  ],
  "author": "Neumenon",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Neumenon/cowrie"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "optionalDependencies": {
    "fzstd": "^0.1.1"
  },
  "devDependencies": {
    "@msgpack/msgpack": "^3.1.3",
    "@types/node": "^25.0.9",
    "c8": "^11.0.0",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3"
  },
  "dependencies": {},
  "homepage": "https://github.com/Neumenon/cowrie#readme",
  "bugs": {
    "url": "https://github.com/Neumenon/cowrie/issues"
  }
}
